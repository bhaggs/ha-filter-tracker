# Filter Tracker

Documentation for this integration lives in the repository README, which is the
single canonical copy:

https://github.com/bhaggs/ha-filter-tracker#readme

This file previously held a second, separately maintained copy of those docs.
The two drifted apart — the copy HACS actually renders ended up missing the
runtime-tracking and `set_usage_time` documentation entirely — so there is now
one version only.
